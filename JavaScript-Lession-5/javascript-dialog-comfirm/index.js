// ? Now we learn JavaScript default dialog boxes.

// ? Let's try to the confirm() how is it work ------------------------------------>
confirm('Are you sure ?');

// ? Now we see the power of confirm() dialog.

// ? confirm() also return the true and false value. How? let's go to see this ------------------------------------->
const value = confirm('Are you sure ?');
console.log(value);

// ! note:
// ? it can also return true and false boolean value. when user click the "Ok" button it return "true". when user click the "Cancel" button it return "false" value. good job. 