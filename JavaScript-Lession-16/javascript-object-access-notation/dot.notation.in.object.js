// ? In Javascript have some two way to access the object properties using dot notation and bracket notation ----------------------------------------------------------->
// ? In JavaScript, dot notation and bracket notation are both ways to access properties or methods of an object. ------------------------------------------------------>

// ? let's practice with dot notation ---------------------------------->

const person = {
    name: 'Rakibul Rahman',
    age: 18
};

console.log(`Person Name: ${person.name}`);
console.log(`Person Age: ${person.age}`);

// ! 1. Dot Notation ->
// ? Dot notation is the most common and simplest way to access object properties. You use a period (.) followed by the property name to access it.