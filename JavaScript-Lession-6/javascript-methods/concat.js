// ? today we learn JavaScript 'concat()' string methods ------------------------------------------>

// ? Let's try to JavaScript string methods 'concat()' -------------------------------------------->

const firstName = "Rakibul";
const lastName = "Rahman";

const fullName = firstName.concat(" ", lastName);

console.log(fullName); // output: "Rakibul Rahman";

// ! note:
// ? concat(string1, string2, ...) – Joins two or more strings.