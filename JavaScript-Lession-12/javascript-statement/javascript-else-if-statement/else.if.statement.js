// ? javascript have "else if" statement. this statement is combine and working with "if" statement ------------------------------------->

// ? let's try and practice "else if" statement ---------------------------------------------------->
// ? syntax of "else if" statement ------------------------------------->

const truth = true;
const falsy = false;

if (truth) {
    console.log("hello");
} else if (falsy) {
    console.log('hey');
};

// ! note: 
// ? after "if" condition fall to execute "if" (statement) block code than go to second condition "else if" (statement) than check "else if" (statement) code.