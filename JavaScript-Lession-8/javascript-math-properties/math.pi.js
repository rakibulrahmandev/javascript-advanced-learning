// ? javascript have some math properties. today we learn Math.PI properties --------------------------->

// ? let's try "Math.PI" properties --------------------->

const pi = Math.PI;

console.log(pi); // output: 3.141592653589793;

// ! note:
// ? when we need to get pi value for some math operation then we get pi value easily using Math.PI properties;