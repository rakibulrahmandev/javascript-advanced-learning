// ? javascript have some math properties. today we learn Math.SQRT2 properties --------------------------->

// ? let's try "Math.SQRT2" properties --------------------->

const sqrt = Math.SQRT2;

console.log(sqrt); // output: 1.4142135623730951;

// ! note:
// ? when we need to get "SQRT2" value for some math operation then we get "SQRT2" value easily using Math.SQRT2 properties;