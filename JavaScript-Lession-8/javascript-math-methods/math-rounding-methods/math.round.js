// ? javascript have some math methods. today we learn Math.round methods --------------------------->

// ? let's try math methods Math.round(x) ------------------>

const num1 = 1.0254;
const roundNum1 = Math.round(num1);

const num2 = 2.5645;
const roundNum2 = Math.round(num2);

console.log(roundNum1); // output: 1;
console.log(roundNum2); // output: 3;

// ! note: 
// ? Rounds to the nearest integer;