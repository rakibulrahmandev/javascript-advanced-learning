// ? javascript have some math methods. today we learn Math.trunc properties --------------------------->

// ? let's try math methods Math.trunc(x) ------------------>

const num1 = 2.5458;
const truncNum1 = Math.trunc(num1);

const num2 = 3.6545;
const truncNum2 = Math.trunc(num2);

console.log(truncNum1); // output: 2;
console.log(truncNum2); // output: 3;

// ! note:
// ? Removes decimal part (returns integer);