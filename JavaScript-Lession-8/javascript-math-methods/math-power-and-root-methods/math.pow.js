// ? javascript have some math methods. today we learn Math.pow properties --------------------------->

// ? let's try math methods Math.pow(base, exp) ------------------>

const num2ToPowerOfNum3 = Math.pow(3, 2);
const num8ToPowerOfNum4 = Math.pow(4, 8);

console.log(num2ToPowerOfNum3); // output: 9;
console.log(num8ToPowerOfNum4); // output: 65536;

// ! note:
// ? Raises a number to a power;