//? JavaScript let variable ------------------------------------------------>
let my = "Rakibul Rahman";
let age = 18;
let happy = true;

console.log(my);
console.log(age);
console.log(happy);

//? here some briefing of "let" and some key feature and key point to highlight to know about this.

// note: JavaScript "let" variable using in the JavaScript latest version like ES6 - 2015 and upper version.
//**
// ? Block-scoped: The variable is only available inside the block {} where it is declared.
// ? Can be reassigned but not redeclared in the same scope.
// ? Hoisted but not initialized: Accessing it before declaration results in a ReferenceError.
//  */